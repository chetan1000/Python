# write a function to find maximum of two numbers
def findMax(x,y):
    if x>y:
        return x
    else:
        return y


result = findMax(3,5)
print(result)


 # return is keyword, return would send the value back & on that value, if we want to do some conditional evaluevation we can do it
 

